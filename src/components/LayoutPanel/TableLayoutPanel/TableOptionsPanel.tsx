import { UseFormReturnType } from "@mantine/form";
import { type TableOptions } from "../../../hooks/useOptions";

export type TableOptionsPanelProps = {
  form: UseFormReturnType<TableOptions>;
};

export function TableOptionsPanel(
  {
    // form
  }: TableOptionsPanelProps,
) {
  return <>TODO...</>;
}
